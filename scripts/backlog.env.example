# Copy to repo-root `.env` (gitignored) and set a token with Issues + Metadata access.
# Do not commit real tokens.

GITHUB_TOKEN=ghp_your_token_here

# Optional: override target (default MetanoicArmor/I2PChat)
# GITHUB_REPOSITORY=owner/repo
